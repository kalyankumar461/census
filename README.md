# census

Clone the code in the local by navigating to the selected folder in git cmf line 
git clone https://github.com/kalyankumar461/census.git

After cloning go inside the folder by using cd census
checkout the selected branch by using git checkout development

Modify the application properties to the local postgres db set up